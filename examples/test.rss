/* broken:
^=
if statements
switch statements
function declarations (return statements are fine)

other issue:
strings parse as Var("string")
*/

let x:int = 0;
for(x; x < 10; x = x + 1) {
    let y:int = 0;
    y = y + 1;
    y -= 1;
}